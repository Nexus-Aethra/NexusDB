[package]
name = "nlog"
version = "0.1.0"
edition = "2024"

[dependencies]
scheduler = { path = "../scheduler" }
crossbeam-queue = "0.3"

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2"

[dev-dependencies]
tempfile = "3"
